on: [pull_request]

jobs:
  benchmarking:
    runs-on: ubuntu-latest
    name: Benchmarking the generators

    steps:
      - uses: JayaKrishnaNamburu/behchmark@development
        with:
          secret-token: "${{ secrets.BENCHMARK_TOKEN }}"
